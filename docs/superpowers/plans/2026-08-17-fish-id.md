# AI 识别鱼种 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 拍照/上传渔获图，识别上海对象鱼中文名，可一键钉到高德。

**Architecture:** 浏览器压缩图片 → 本机 `POST /api/fish-id`（Key 留在 .env）→ 视觉模型 JSON → `normalizeFishName` 词表校验 → FishIdPanel；可选写入报渔获。

**Tech Stack:** 现有 Vite 中间层、OpenAI 兼容多模态、Vitest

---

规格：`docs/spec/FR-7-fish-id.md`、`docs/spec/PRD.md` FR-7、`docs/spec/SDD.md` §10。

### Task 1: 词表与归一化（TDD）

**Files:**
- Create: `src/lib/fishId/catalog.ts`, `src/lib/fishId/catalog.test.ts`

- [x] **Step 1:** `FISH_CATALOG` 含鲫鲤草青鳊鲈翘嘴白条黑鱼黄颡等
- [x] **Step 2:** `normalizeFishName` 别名映射；未知返回 `不确定`
- [x] **Step 3:** `npm test` 覆盖乌鳢/黄辣丁/花鲈/largemouth 不在词表则不确定

### Task 2: 本机识别 API

**Files:**
- Modify: `src/vite-plugin-ingest.ts`
- Create: `src/lib/fishId/server.ts`

- [x] **Step 1:** `POST /api/fish-id` 收 `{ imageBase64, mime }`
- [x] **Step 2:** 无 `FISH_ID_API_KEY` 返回 503 `not_configured`
- [x] **Step 3:** 有 Key 则调多模态，解析 JSON，再 `normalizeFishName`
- [x] **Step 4:** `.env.example` 增加 `FISH_ID_API_KEY` / `FISH_ID_MODEL` / `FISH_ID_BASE_URL`

### Task 3: UI 与上报衔接

**Files:**
- Create: `src/components/FishIdPanel.tsx`
- Modify: `src/App.tsx`, `src/index.css`

- [x] **Step 1:** 上传/拍照、预览、识别中、结果与候选
- [x] **Step 2:** 「用这个鱼名上报」调用现有 `persistReport`
- [x] **Step 3:** 无 Key 时手动点选词表

### Task 4: 验证

- [x] **Step 1:** `npm test` 与 `npm run build`

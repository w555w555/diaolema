# progress.md

## 2026-08-17

- 开启 Spec + SDD 规则。
- 写入 PRD / SDD / 实施计划。
- 开始实现 MVP 代码。
- `npm test` 8 passed；`npm run build` 成功。
- 开发服务器：`npm run dev`。
- 已配置高德 Key；补齐地图内路线规划与高德导航跳转。
- 标点附文改为「谁 多久前 钓到了什么鱼」，常显在高德钉下方。
- 小红书/抖音作为来源标签 + 分享口令入库；不登录爬全站。
- 把用户提供的 `save_post` 做成 Windows 本机软件：`启动钓了嘛.bat` 打开独立窗口，规则提取 + 可选 AI 补空 + SQLite。
- `npm test` 25 passed；`npm run build` 成功。
- 对小红书/抖音/微博链接不抓登录墙页面。
- 拒绝自动爬小红书/抖音/微信公众号；入库成功后把 JSON/Markdown 存到 `data/captures/`。
- 按 `fish_scout.config.yaml` 做每日公开发现：搜索引擎标题/摘要/链接，登录墙不读正文，再入库生成日报。
- 项目接入三个公开发现 skill：`baidu-search`、`multi-search-engine`、`defuddle`；命中必须匹配当前查询站点。
- 增加上海大众点评公开渔场目录（人均/状态），地图钉「场」标点。
- 营地图标改为内嵌 SVG data URI；原先 public SVG 含损坏中文属性，浏览器无法渲染。
- 写入 FR-7 AI 识鱼 Spec / SDD，实现尚未开始。
- AI 识鱼已落地：左侧「识鱼」面板、本机 `/api/fish-id`、词表归一化；`npm test` 58 passed。
- 策略页味形与标点随目标鱼、台钓/路亚、气压气温时段重算；规则来自公开垂钓文，运行时不联网。
- 路亚拟饵按对象鱼拆开：种类 + 克数/尺寸 + 颜色 + 操法，编译自渔夫者/钓鱼007、渔钓者、酷钓鱼、酷米网；`planLurePick` 可单测。
- 今日天气栏增加晨昏窗口倒计时：`windowCountdown`，晨窗 05–08、昏窗 17–20，每秒刷新。
- 策略首页精简：去掉常用工具与目标鱼说明卡；方案格只留味形/标点；点鱼名换鱼、点天气条看详情。
- 目标鱼按钓法筛选：台钓鲫鲤草青等，路亚鲈翘黑鳜等，白条罗非鲶塘鲺兼钓；切换钓法自动改默认鱼。
- 首页识鱼下方增加钓友分享：鱼种、钓点、时间、来源与封面；无实拍用鱼种缩略图。
- 写入示例短文与本地封面图（`public/shares/`），卡片标明「示例」，点开可读全文。
- 钓友分享改为下半屏独立竖向列表：策略区（天气/方案/识鱼）固定在上半，分享区自滚动，封面在上、文案在下。
- 分享流改为大众点评式双列 1:1 方形封面；卡片只留标题与鱼种/钓点，示例角标打在图上。
- 手机壳内隐藏滚动条（仍可滑动）。
- 分享卡去掉底色描边：无框圆角方图，鱼种胶囊压左下，示例黑底白字，标题一行，辅文只留钓点与时间。
- 分享封面加底部淡渐变与点按 `scale(0.98)`；无实拍改为深色底加大号鱼名。
- 分享区头改为「今日渔获 · 条数 / 全部 ›」细金线；详情顶图叠作者时间，正文去示例声明，去钓点改全宽青绿。
- 分享流改为双列瀑布流；本机点赞/关注作者（localStorage），不做粉丝列表与私信。
- 首页品牌行接入 `public/logo.svg`，产品名改为渔见（Logo 同款字族与渐变字色）。进入应用先显示 Logo 开屏。
- 修复 Logo 不可见：去掉会吃掉金鱼的 SVG 滤镜，标题改实色，开屏底比图标更深，图标走打包路径。
- 开屏弹窗加金青绿光晕、水纹、Logo 淡入与「轻触进入」。

## 2026-08-18

- 钓点地图去掉长附文和渔获气泡，只留渔见评分水滴小标记；无反馈显示「新」。
- 点钉打开钓点详情：客户反馈图文、近期渔获、本机 1–5 星写评；评分等于星级均分（一位小数），不沿用点评星。
- 钓场排行显示反馈条数；点条目进入本机详情，不再打开钓鱼之家网页。
- 渔圈首页改为入口条 + 赛事/装备/群聊/技巧预览，避免五块空磁贴。
- 今日方案点「介绍」可看鱼类习性与技巧；窗口倒计时改为距晨间窗口 / 距黄昏窗口。
- 手机定位：开屏结束后、钓点、+ 页请求浏览器定位；失败说明原因并回落人民广场。
- 首页 AI 识鱼与 + 报渔获均可拍照（系统相机 / 安全上下文实时取景）；识鱼上报可带压缩封面。
- 增加 Zeabur 部署：控制台用 GitHub；本机用 `部署到Zeabur.bat` / `npx zeabur@latest deploy`。
- AI 识鱼改到底栏中间金色按钮（按钮上写 AI 识鱼，去掉 + 和下方「识鱼」）；首页只留天气、方案、今日渔获。
- 钓点「打开高德地图」改为可点 `uri.amap.com` 链接，避免手机拦截 `window.open`；详情/排行同样用真链接。
- 「我的」页底部留空约 96px，分享入库菜单与表单不再被金色按钮挡住。
- 钓点地图：等高德 SDK 在可见容器里加载，8 秒超时改演示底图，避免一直「正在加载地图」。
- 手机网页「我的」按小视口留白，分享入库提到菜单最上，避免被底栏挡住。
- AI 识鱼已配置时去掉豆包文案；识别弹出扫描窗，成功打钩。
- 「我的」增加可选 Supabase 邮箱登录；secret key 不进前端。
- 登录后点头像可改昵称与头像（相册压成 JPEG 存本机）；渔圈群聊发送可用，作者取当前昵称。
- 公网群聊第一期已落地。
- 上报渔获：钓点为空会提示；底栏不再挡住「上报渔获」；成功后跳到「我的」渔获记录。
- 登录后资料/渔获/点赞关注/想买/评测写入 Supabase；需再跑 `supabase/user_data.sql`。
- 粉丝互关私聊：粉丝名单可开关「允许私聊」；互关且双方打开后出现「私聊」。消息表 `dm_messages`；需在 SQL Editor 跑 `supabase/dm.sql` 全文。`directChat.test.ts` 4 passed。
- 注册/登录只填邮箱密码。项目地址由部署环境或 `/api/public-config` 提供，不再让用户填。
- 登录成功回到「我的」首页；群聊带头像/名片/@/主页。
- 内容互动：渔获评论、转发到主题群或复制文案、关注列表随操作变化、点作者进主页。
- 粉丝名单每条常显「私聊」；去掉菜单里重复的「我的粉丝」。群聊头像/关注/@更明显，输入栏不再撑满屏幕。

## 2026-08-19

- 群聊/私聊转发改为渔获卡片：正文带 `#yj:id`，点卡片打开原详情；无 id 的旧转发按作者+钓点+鱼种匹配。路亚夜聊加一条示例转发。
- 修复转发进群不可见：转发始终写入本机，群聊把本机用户消息与公网消息合并显示。
- 钓场排行加深：类型芯片、营业中、离我近与距离；详情附近 3 场与更稳的渔获匹配。地图仍只显示营地钉。
- 举报/拉黑：按作者名本机生效；今日渔获、评论、群气泡与私信会话隐藏；「我的 → 已拉黑」可解除。约钓/支付本轮不做。
- 今日渔获支持最多 9 张图与一条 15 秒短视频；封面用第一张，多图/视频有角标；详情可滑图或播放。报渔获相册可多选。data URL 不写飞书。
- 登录后短视频上传到 Supabase Storage（公开桶 `yj-media`）；群聊/私聊写 `media_url`，渔获写 `video_url`。未登录仍本机预览。须在 SQL Editor 再跑一遍 `supabase/social.sql`。`npx vitest run` 225 passed；`npx tsc --noEmit` 通过。
- 群聊/私聊可引用回复；消息页可搜会话，群内可搜口讯并跳转；再次进入有未读时出现「以下为新消息」。不做群已读回执。`npx vitest run` 233 passed。

## 2026-08-19

- 登录后拉回点赞、关注、想买、评测、拉黑、私聊开关与评论，与本机并集，不覆盖离线写入。渔获详情评论写入 `share_comments`。粉丝名单用 `canOpenFanChat`，不为非示例粉丝自动打开允许。私信订 `dm_messages` Realtime，历史读双方 thread。示例封面落到 `public/shares/` 与 `public/spot-photos/`。须再跑一遍 `supabase/social.sql`（或 `dm.sql`）以打开私信双向可读与 Realtime。`npx vitest run` 250 passed；`npx tsc --noEmit` 通过。

## 2026-08-20

- 今日方案加「理由」打开已有 AdvicePanel。地图选点同时改天气坐标并重算方案。登录后渔获多图与聊天图片上传 `yj-media`，行只存 HTTPS。须再跑 `supabase/social.sql` 加上 `image_urls` 与图片 MIME。`npx vitest run` 253 passed。
- 天气详情接 Open-Meteo 24 小时与 7 天；头像与聊天语音登录后上云；钓场可收藏；自己的渔获可改删。地图仍只画营地钉。须再跑 `supabase/social.sql`（`venue_favs`、渔获 DELETE、音频 MIME）。`npx vitest run` 264 passed；`npx tsc --noEmit` 通过。
- 策略首页换成 OLED 仪器盘（绿指数 / 蓝气象 / 琥珀窗口）。今日渔获挪到渔圈；金青绿上一版存档在 `_archive/ui-gold-green/`。不编造溶氧水温。

- 建议引擎改国内淡水口径：气压急降/低压改为口差找氧（指数 -16/-12），高压稳压宜守底（+8），升压回口（+10）；盛夏正午默认底层，台钓草/鳊改中上层；加州鲈/大口黑鲈归词表鲈鱼。晨昏窗口与预报小时按上海东八区，不跟运行环境本地时区。不用欧美降压抢食。不编造溶氧水温，不扩到斤塘或实时鱼情排行。`npx vitest run` 273 passed；`npx tsc --noEmit` 通过。
- 按上一轮结论二次检索国内公开钓技并编译：上海低压 ≤1005 hPa；公园浅湖翘嘴白天 5–7g（大水面可 7–12g）；黑鱼中鱼后等两三秒再抽；花鲈潮沟/闸口看潮；盛夏夜黄颡走障碍；只提示指定钓点，不写论坛野岸 GPS。`npx vitest run` 274 passed；`npx tsc --noEmit` 通过。
- 自我检视：修正凌晨五点与黄昏被当成夜钓勺型、草鱼夏天颗粒被搓饵盖掉、盛夏正午叠加低压仍死守亮水底；补鲻鱼落潮与花鲈半咸水生境。`npx vitest run` 278 passed；`npx tsc --noEmit` 通过。
- 水域加入收费塘；水色独立算法（黄绿最好开口，泥浆/恶水口差）；收费塘刚换水/调水/老水酱层改指数与饵形。不编造溶氧水温，不做斤塘。`npx vitest run` 292 passed；`npx tsc --noEmit` 通过。
- 把 OLED 仪器盘首页与水域/水色/塘保养合在一起：策略页保持指数环与气象短卡，方案卡内点选水域与水色。`npx vitest run` 298 passed；`npx tsc --noEmit` 通过。

## 2026-08-21

- 风力改国内几级：界面不再写 km/h；4 级只改战术（加重饵/侧风岸），5 级才减指数；盛夏 0–1 级易闷、湿度 ≥85% 且 ≤2 级记闷湿。窗口针按东八区。示例标题去掉「抢食」。不编造溶氧，河口潮汐仍无数据故不写进指数。`npx vitest run` 302 passed；`npx tsc --noEmit` 通过。

## 2026-08-21 水色研究

- 水色开口查表 `WATER_BITE_SCORE` 写进 `buildFishingIndex`（输出 `waterBite`），首页水色下显示开口程度；黄绿芯片含茶褐。不编造 NTU/溶氧。`npx vitest run` 306 passed；`npx tsc --noEmit` 通过。

- 写入 FR-7 AI 识鱼 Spec / SDD，暂未实现代码。
